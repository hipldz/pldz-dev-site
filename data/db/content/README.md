# Content DB

Persistent article view counters and comments. The rebuildable article index is
stored under `data/cache/article-index/`. DB records are accessed through Content
services and are never served as raw files.
